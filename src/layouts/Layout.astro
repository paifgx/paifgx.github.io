---
import MainNavigation from "../components/layout/MainNavigation.astro";
import Footer from "../components/layout/Footer.astro";

interface Props {
  title: string;
}

const { title } = Astro.props;
---

<!doctype html>
<html lang="de">
  <head>
    <meta charset="UTF-8" />
    <meta name="description" content="Astro description" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <meta name="generator" content={Astro.generator} />

    <link rel="icon" type="image/svg+xml" href="/favicon.svg" />

    <title>Patrik Garten · Zukunft sichern, heute!</title>
  </head>
  <body class="dark:bg-woodsmoke bg-alice-blue">
    <MainNavigation />

    <main>
      <slot />
    </main>

    <Footer />
  </body>
</html>

<style is:global>
  :root {
    --accent: 136, 58, 234;
    --accent-light: 224, 204, 250;
    --accent-dark: 49, 10, 101;
    --accent-gradient: linear-gradient(
      45deg,
      rgb(var(--accent)),
      rgb(var(--accent-light)) 30%,
      white 60%
    );
  }

  html {
    font-family: system-ui, sans-serif;
    scroll-behavior: smooth;
  }

  h1 {
    @apply text-tiber dark:text-granny-apple text-5xl font-bold;
  }

  h2 {
    @apply text-tiber dark:text-granny-apple text-4xl font-bold;
  }

  h3 {
    @apply text-tiber dark:text-granny-apple text-3xl font-bold;
  }

  h4 {
    @apply text-tiber dark:text-granny-apple text-2xl font-bold;
  }

  h5 {
    @apply text-tiber dark:text-granny-apple text-xl font-bold;
  }

  h6 {
    @apply text-tiber dark:text-granny-apple text-lg font-bold;
  }

  section {
    @apply text-woodsmoke dark:bg-woodsmoke flex flex-col justify-center bg-alice-blue px-8 py-16 dark:text-alice-blue;
  }

  .btn,
  button {
    @apply rounded px-6 py-3;
  }

  .btn-primary {
    @apply bg-tiber hover:bg-tiber-900 text-alice-blue;
  }

  .btn-secondary {
    @apply bg-gray-700 text-alice-blue hover:bg-gray-800;
  }

  .btn-outline {
    @apply border-tiber text-tiber hover:bg-tiber-900 border hover:text-alice-blue dark:border-alice-blue dark:text-alice-blue;
  }

  .btn-info {
    @apply bg-amaranth hover:bg-amaranth-900 text-alice-blue hover:text-alice-blue;
  }
</style>
