---
interface Props {
  class?: string;
}

const { class: className } = Astro.props;
---

<p
  class={`text-xs font-semibold uppercase tracking-[0.3em] text-brand-gold dark:text-brand-gold ${className || ""}`}
>
  <slot />
</p>
