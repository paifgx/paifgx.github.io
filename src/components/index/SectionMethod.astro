---
import SectionSubtitle from "../ui/SectionSubtitle.astro";
import { processSteps } from "../../data/home";
---

<section class="section-base">
  <div
    class="absolute inset-0 -z-10 bg-[radial-gradient(#2563eb_1px,transparent_1px)] bg-size-[32px_32px] opacity-[0.02] dark:opacity-[0.05]"
  >
  </div>

  <div class="mx-auto max-w-7xl relative z-10">
    <div class="mx-auto max-w-2xl text-center mb-10 sm:mb-12 lg:mb-16">
      <SectionSubtitle>Vorgehensweise</SectionSubtitle>
      <h2
        class="mt-2 text-2xl sm:text-3xl font-bold tracking-tight text-gray-900 dark:text-white lg:text-4xl"
      >
        Struktur statt Chaos.
      </h2>
      <p
        class="mt-3 sm:mt-4 text-base sm:text-lg leading-7 sm:leading-8 text-gray-600 dark:text-gray-300"
      >
        Mein Prozess sorgt dafür, dass Sie schnell Ergebnisse sehen, ohne
        technische Schulden für die Zukunft aufzubauen.
      </p>
    </div>

    <div class="grid grid-cols-1 gap-6 sm:gap-8 md:grid-cols-2 lg:grid-cols-4">
      {
        processSteps.map((step) => (
          <div class="group relative flex flex-col">
            <span class="text-5xl sm:text-6xl font-bold text-gray-200 dark:text-white/5 mb-3 sm:mb-4 group-hover:text-brand-blue/10 transition-colors duration-300">
              {step.id}
            </span>
            <h3 class="text-lg sm:text-xl font-semibold leading-7 sm:leading-8 text-gray-900 dark:text-white relative z-10 -mt-6 sm:-mt-8 ml-2 group-hover:text-brand-blue dark:group-hover:text-brand-gold transition-colors">
              {step.name}
            </h3>
            <p class="mt-3 sm:mt-4 text-sm sm:text-base leading-6 sm:leading-7 text-gray-600 dark:text-gray-300">
              {step.description}
            </p>
          </div>
        ))
      }
    </div>

    <p
      class="mt-8 sm:mt-10 lg:mt-12 text-center text-sm sm:text-base text-gray-500 dark:text-gray-400"
    >
      Kein PoC-Friedhof, kein Shadow-IT-Monster, das niemand verantworten will.
    </p>
  </div>
</section>
