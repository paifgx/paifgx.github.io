---
import Svg from "@cxa/astro-inline-svg";

interface Props {
  text: string;
  raw: string;
  link: string;
  id: string;
}

const { text, raw, link, id } = Astro.props;
---

<a
  href={link}
  rel="noopener noreferrer"
  target="_blank"
  aria-label={text}
  title={text}
>
  <p class="sr-only">{text}</p>

  <Svg
    class="text-woodsmoke hover:text-tiber dark:hover:text-granny-apple dark:text-alice-blue"
    raw={raw}
    height={30}
    width={30}
  />
</a>
